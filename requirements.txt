# Core runtime dependencies
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
pyarrow>=14.0

# Development / testing
pytest>=7.0

# MetaTrader 5 integration (Windows only — the official package wraps the MT5
# terminal DLL and cannot be installed on Linux/macOS).
# On Windows: pip install MetaTrader5
# MetaTrader5>=5.0.45
